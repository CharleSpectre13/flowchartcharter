# CXR — Halt Law v2.5.0

**Skills:** superpowers · flowchartcharter-engineering · loop-engineer · rhythm-marker-validator · graphify · graphrag-pipeline · pep8-python-code-reviewer  
**Building after line-by-line + research.** GraphRAG not reinvented.

## What completed
Process KillSwitch bound into `ActionUnit.execute` (unique chokepoint). Charter, playbook, API, and live LLM refuse when HALTED. Playpen forces dry-run. Halt + notebook persist when enabled. Dry-run quality is earned 0.90. Retrieval stamp stays honest.

## Excellent
Direct `ActionUnit.execute()` after HALT is blocked. That was the real bypass.

## Weak
Sandbox is still policy, not a kernel. Persist is file JSON, not a WAL. Isolation score is still a number.

## Do's tested
- HALT unavoidable without ToolPort (V1)
- Charter / playbook refuse (V2, V3)
- Restart stays HALTED (V4)
- `dry_run=False` cannot un-cage (V5)
- Notebook disk (V6)
- No 0.92 gift (V7)
- `claimed_graphrag` false (V8)
- v2.0–v2.4 suites still green

## Other divisions
Do not market “world-class isolation.” Market “the stop button is on the door, not the hallway.”

## Next cycle
Process/container playpen if a customer will actually run side-effects live. Not before.

**Grade movement (honest):** 5.5 → **6.5 / 10** as an accountable agent OS. GraphRAG still **8 / 10** at retrieval. Different sports.
