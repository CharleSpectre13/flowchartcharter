# CXR — Next stage determination (no build)

**Skills:** continuous-team-audit-loop · loop-engineer · rhythm-marker-validator · flowchartcharter-engineering

## Current
OS **8.1**. FCC retrieval **7.4**. GraphRAG corpus **8**. Global search is still `join(community titles[:4])`.

## Weak
No query-focused summarization. Episodes are findable as passages; themes are not synthesized. Leiden-as-GraphRAG would be a lie.

## Determination
**v3.0 Extractive QFS** — map-reduce over valid TextUnits by `fcc_community`. Backend `fcc_qfs`. GraphRAG HTTP remains the only `claimed_graphrag`.

## Do not do next
Leiden-in-core. Live LLM reduce without a key. gVisor.
