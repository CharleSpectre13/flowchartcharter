# FlowChartCharter Engine — production microservice deps
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
# optional live LLM (stdlib urllib used by default; httpx if preferred)
# httpx>=0.27.0
