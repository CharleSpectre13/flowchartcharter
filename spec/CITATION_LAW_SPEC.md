# SPEC — Citation Law + Episode Bind (v2.8.0)

## Outcomes

1. `execute_charter` writes one episode TextUnit (`full_rebuild=false`).
2. Every retrieval hit has `source`. Empty source → `ungrounded_hit`.
3. `mode=fusion` merges muscle + delta + lazy. SIMPLE stays muscle-only.
4. Alias resolve merges `DeltaNotebook` / `delta_notebook`.
5. `claimed_graphrag` still requires HTTP.

## Out

Leiden-as-GraphRAG. Live LLM extract. Vendor memory SDK.
