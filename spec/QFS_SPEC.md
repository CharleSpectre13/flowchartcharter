# SPEC — Extractive QFS (v3.0.0)

## Outcomes

1. Global/theme with valid TextUnits uses `fcc_qfs` (map-reduce sentences).
2. Fresh KG (no units) stays `fcc_kg_subflow` title path — prior suites green.
3. Theme A query does not cite theme B units.
4. Synthesis ≠ join(community titles).
5. `claimed_graphrag` still requires HTTP.

## Out

Leiden-as-GraphRAG. Live LLM reduce without a key.
